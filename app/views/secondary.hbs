<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
      {{>header}}
      <link rel="stylesheet" href="stylesheets/{{file}}.css" />
      <script src="javascript/{{file}}.js"></script>
  </head>
  <body>
    {{{body}}}
  </body>
</html>
